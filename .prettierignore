# Generated by tools/themes/generate.mjs — formatting them here would drift from the generator's
# byte-exact output and be reverted on the next `npm run themes:generate`.
src/themes.css
src/lib/themes/palettes.ts

# Generated by tools/registry/generate.mjs, for the same reason.
registry.json

# Hand-formatted documentation: tables and column alignment are deliberate.
*.md

# Tooling state and vendored agent skills — not the library's code.
.claude/

# Tool-owned lock files: whoever writes them wins, so formatting them here only causes churn.
package-lock.json
skills-lock.json

dist/
